import * as React from "react"
import {useEffect} from "react"


const ActivityContext = React.createContext()

export default function ActivityContextProvider(prop) {
    // for later

    return (
        prop
    )

}